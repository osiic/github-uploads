{
  "name": "uploads",
  "module": "index.ts",
  "src": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun-vercel index.ts",
    "buildCommand": "bun run build",
    "start": "bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "bun-vercel": "^1.0.0-alpha.5"
  }
}
